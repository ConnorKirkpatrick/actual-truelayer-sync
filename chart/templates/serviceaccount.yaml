{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ default (include "actual-truelayer-sync.fullname" .) .Values.serviceAccount.name }}
  labels:
    {{- include "actual-truelayer-sync.labels" . | nindent 4 }}
{{- end }}
